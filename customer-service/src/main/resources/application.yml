spring:
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/customer
    username: postgres
    password:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  flyway:
    url: jdbc:postgresql://localhost:5432/customer
    user: postgres
    password: